# Strings = a sequence of characters to be read by the user
myString = "Some text"
print("Variable MyString =",type(myString))
# Strings are always in quoatation marks
myString = "33"
# Determing Data Type using the type() function
print("Variable MyString =",type(myString))
# Index of String - Use braces to determine the index of the string
myString[0]
myString[-1]
# length of a string (index length)
len(myString)


